# Server deployment settings — copy to .env and fill in your values.
# This file is loaded by the Makefile.

# SSH destination for deployment (user@host)
VPS_HOST=user@example.com

# Domain name for the site
SITE_DOMAIN=food.example.com

# Absolute path on the server where the site is served from
VPS_PATH=/home/user/food.example.com

# Absolute path to the project repo on the server
VPS_REPO=/home/user/nyc-eats

# --- Cross-reference API keys ---
# Yelp Fusion API key (get one at https://www.yelp.com/developers/v3/manage_app)
YELP_API_KEY=

# Google Places API key (must have Places API enabled)
# NOTE: Google is pre-filled locally — not needed on the server.
GOOGLE_API_KEY=

# OpenTable: no API key needed (uses public GQL endpoint)

# Daily request limits (optional, defaults shown)
# YELP_DAILY_LIMIT=4500
# GOOGLE_DAILY_LIMIT=1000
# OPENTABLE_DAILY_LIMIT=2000
